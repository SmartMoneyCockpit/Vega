# Vega Cockpit — Unified requirements (Render + Actions)
# Python 3.10 runtime pinned via runtime.txt

# --- UI Core ---
streamlit>=1.36
pandas==2.2.2
numpy==2.0.2
PyYAML==6.0.2
streamlit-lightweight-charts==0.7.20
flask==3.0.3
gunicorn==23.0.0  # For production server on Render
uvicorn[standard]==0.30.6


# --- HTTP / Async Utils ---
requests>=2.32.0,<3
httpx==0.27.2
nest-asyncio==1.6.0
python-dateutil==2.9.0.post0

# --- Email (A+ Digest, Alerts) ---
sendgrid==6.11.0

# --- Charts / Exports (PNG + PDF Snapshots) ---
matplotlib>=3.8,<3.10
reportlab>=4.1,<5

# --- Data Providers / Market Feeds ---
yfinance==0.2.40
polygon-api-client==1.13.6
