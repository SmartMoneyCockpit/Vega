# One-and-Done (v1)

- Export **watchlists (TXT)**, **trades (CSV)**, **deep links (CSV)**.
- **Exchange normalization** (NYSE/NASDAQ/TSX/TSXV/BMV/TSE/XETR/LSE).
- **Inline TradingView preview** expander.
- **Open All in TradingView** button (staggered to avoid popup blocks).
- **No TradingView secrets** required; one-way only.
- Safe imports (`__init__.py`) and Streamlit control fallback.
