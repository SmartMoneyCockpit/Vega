name: Econ Calendar

on:
  workflow_call:
    inputs:
      enabled:
        type: string
        required: false
        default: 'true'

permissions:
  contents: read

jobs:
  build:
    if: ${{ inputs.enabled == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with your real steps
      - name: precheck
        run: echo "econ_calendar / precheck ok"

      - name: build
        run: echo "econ_calendar / build running"
