name: Uptime Ping

on:
  workflow_call:
    inputs:
      enabled:
        type: string
        required: false
        default: 'true'

permissions:
  contents: read

jobs:
  build:
    if: ${{ inputs.enabled == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with your real steps
      - name: precheck
        run: echo "uptime_ping / precheck ok"

      - name: ping
        run: echo "uptime_ping / ping running"
