name: Color Guard

on:
  workflow_call:
    inputs:
      enabled:
        type: string
        required: false
        default: 'true'

permissions:
  contents: read

jobs:
  build:
    if: ${{ inputs.enabled == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with your real steps
      - name: precheck
        run: echo "color_guard / precheck ok"

      - name: build
        run: echo "color_guard / build running"
